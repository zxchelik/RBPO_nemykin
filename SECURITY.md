# 🔐 Security Policy

## Поддерживаемые версии
Проект находится в активной разработке (MVP-стадия).
Безопасность критически важных компонентов (аутентификация, доступ к задачам) поддерживается с приоритетом.

| Версия | Поддержка безопасности |
|--------|--------------------------|
| main (ветка разработки) | ✅ активно поддерживается |
| старые ветки / форки    | ❌ не поддерживаются |

---

## Уязвимости
Если вы обнаружили уязвимость, пожалуйста:
1. **Не создавайте публичный issue.**
2. Напишите напрямую на почту: mishany1105@gmail.com

Мы постараемся ответить **в течение 72 часов** (SLA).
После подтверждения проблемы мы согласуем шаги по исправлению и публикации.

---

## Сценарии безопасности
- Все задачи **изолированы по пользователям**: никто не может читать или модифицировать чужие записи.
- Доступ к API защищён через JWT-токены.
- Пароли пользователей хранятся в захэшированном виде (bcrypt/argon2).
- Данные задач валидируются на уровне API (статусы, приоритеты).
- Конфиденциальные данные (секреты, ключи JWT, DSN БД) лежат в `.env` и **не коммитятся в репозиторий**.
- Логи (в `logs/`) не должны содержать персональные данные и JWT-токены.
